# چک‌لیست کارهای باقی‌مانده (نسخه‌ی نهایی)

> **وضعیت: مقاله نهایی و قابل ارائه است.** فایل تک‌پارچه‌ی `مقاله_نسخه_نهایی.html` (بدون نیاز به اینترنت؛ در مرورگر باز کن و برای PDF از «Print → Save as PDF» استفاده کن) و بسته‌ی بازبینی `maghale-preview.zip` هر دو به‌روزند.
> نام نویسندگان: امین حسین سدیدی، سید محمد پارسا مولایی طبری، مالک باقری هارونی (دانشگاه اصفهان).
> موارد نیازمند لومریکال دیگر «الزامی» نیستند: عدم‌قطعیت‌های مربوط، صادقانه در جدول ۴ مقاله گزارش شده‌اند. اگر روزی دسترسی پیدا شد، بخش «اختیاری» پایین را ببین.


> وضعیت در تاریخ ۱۴۰۵/۰۶/۲۰. ستون «کی» می‌گوید کار به دوستِ لومریکال‌دار نیاز دارد یا خودت می‌توانی انجام بدهی.

## ✅ کارهایی که در همین بازبینی انجام شد (نیازی به تکرار نیست)

| # | کار | کجا |
|---|-----|-----|
| ۱ | همه‌ی اعداد مقاله به اجرای دوم (۱۴۹۰ / ۱۱۹ / کران ۸٪) به‌روز شد و اعداد نسخه‌ی اول (۲۰۲۰/۱۴۳) به‌عنوان «اجرای مستقل پیشین» با اختلاف ۲۶٪ گزارش شد | `manuscript/main.tex`, `sections/04_results.tex` |
| ۲ | **اعتبارسنجی تحلیلی می** برای کره‌ی D پیاده شد؛ FDTD شکل طیفی را بازتولید می‌کند و ۸–۱۴٪ کم‌برآورد دارد → تبدیل به شکل ۱ و خطای سیستماتیک کمّی | `analysis/mie_dipole_sphere.py`, `figures/D_sphere_mie_vs_fdtd.pdf` |
| ۳ | شکل‌های جدید از داده‌ی خام: طیف همه‌ی حالت‌ها (لگاریتمی)، بازده، مرجع B با η_B، میدان نزدیک با مقیاس مشترک و ماسک منبع | `analysis/make_run2_figures.py`, `figures/fig_*.pdf` |
| ۴ | جدول خلاصه‌ی حالت‌ها + جدول منابع عدم‌قطعیت + جدول حالت‌های اجراشده | `sections/03_methodology.tex`, `04_results.tex` |
| ۵ | «۳۰۰ fs» → «حداکثر ۳۰۰ fs، خاموشی خودکار در ۱۹–۲۸ fs» (طبق لاگ‌ها) | `03_methodology.tex` |
| ۶ | جمله‌ی نادرست «سرستون x_nm اشتباه بود» حذف شد (فایل درست بود) | `04_results.tex` |
| ۷ | پاراگراف تکراری بخش ۲٫۱ حذف و رابطه‌ی LDOS جزئی/تانسور گرین اضافه شد (رفع آدم‌پوشالی «ضرایب اینشتین») | `02_theory.tex` |
| ۸ | مقایسه‌ی منصفانه‌ی «قله با قله» با کره (۲٫۳× و ۲۰×) کنار مقایسه‌ی هم‌طول‌موج | `04_results.tex` |
| ۹ | نتیجه‌گیری کاربردی اصلاح شد (تک‌فوتونی ⇐ گاف ۱۰–۲۰ nm) | `05_conclusion.tex` |
| ۱۰ | مراجع نامرتبط [3] و [22] حذف؛ Mohammadi 2008، Kühn 2006، Ford & Weber 1984، Ruppin 1982، Kim 1988، Mertens 2007، Nikoobakht 2003 اضافه شد | `references.bib` |
| ۱۱ | اصطلاح «تست دوم» از متن حذف شد؛ `\footnote`/`\paragraph`/`p{}`/حروف لهجه‌دار در مبدل HTML پشتیبانی شد | `tools/tex2html.py` |
| ۱۲ | بسته‌ی بازبینی بازسازی شد | `maghale-preview/index.html`, `maghale-preview.zip` |
| ۱۳ | منحنی F_p حالت A از تصویر خروجی با کالیبراسیون محور پیکسلی رقم‌زنی شد (قله ۱۴۷۶ @ ۶۰۸ nm ≈ گزارش ۱۴۹۰) و با برچسب صریح «digitized» در شکل طیف‌ها آمد | `analysis/A_baseline_Fp_digitized.txt` |
| ۱۴ | پویش گاف تحلیلی (۳–۳۰ nm) با می برای کره → شکل جدید + مستندسازی ادعای «گاف ۱۰–۲۰ برای تک‌فوتونی» | `analysis/gap_sweep_mie.txt`, `figures/fig_gap_sweep_mie.pdf` |

---

## ✅ اجرای سوم (دور ۳) — دریافت و در مقاله اعمال شد
- [x] `A_baseline` — F_p=۱۴۹۶@۶۱۰، T=۱۱۹٫۲@۶۱۶؛ همان اعداد اجرای دستی. منحنی رقم‌زنی‌شده حذف شد؛ η_a(λ) کامل A در متن است.
- [x] `G_mesh3`, `G_mesh1.5` — بخش ۴٫۵ جدید «همگرایی مش». **نتیجه ناخوشایند ولی صادقانه:** مکان قله‌ی T پایدار (۶۱۶±۴) اما ارتفاع قله‌ها ±۴۰٪ نوسان دارد (۱۸۴/۱۱۹/۱۶۵). علت: دوقطبی در z=35 با مش ۳/۲/۱٫۵ به‌ترتیب ۲/۱/۰٫۵ nm از نزدیک‌ترین گره فاصله دارد.
- [x] `D_sphere_R15.9_tan` — نسبت شعاعی/مماسی کره در کل طیف ≈ ۲٫۲–۲٫۷ (ثابت)؛ نسبت میله خارج تشدید همان ۲٫۷ و در تشدید ۴۱ → ادعای بخش ۲٫۳ مستند شد (شکل fig_anisotropy).
- [x] `F_gap3/10/20` — شکل fig_gap؛ پویش می فقط برای مقایسه مانده.

## ⚪ اختیاری — فقط اگر روزی به لومریکال دسترسی داشتی (مقاله بدون این‌ها هم کامل است)
- [ ] **اجرای چهارم برای بستن همگرایی مش (مهم‌ترین کار باقی‌مانده):** در اسکریپت، برای حالت A مش موضعی را `1e-9` بگذار (`MESH_DX = 1e-9`) و مطمئن شو `mzmin` مضرب صحیح مش باشد تا دوقطبی (z=35 nm) دقیقاً روی گره بیفتد؛ اگر زمان اجازه داد `0.75e-9` هم. اگر قله‌ی T در مش ۱ و ۰٫۷۵ به هم نزدیک شد (<۱۰٪)، عدد ±۴۰٪ در جدول عدم‌قطعیت را با عدد جدید جایگزین کن و بخش ۴٫۵ را کوتاه کن. زمان تقریبی: مش ۱ nm ≈ ۵–۸ دقیقه.
- [ ] **جایگزین ارزان‌تر:** مش ۲ nm ولی با `mzmin = -21e-9` (به جای -20) — یک بار اجرا؛ اگر عدد به ~۱۶۵ یا ~۲۰۰ پرید، فرضیه‌ی «قرارگیری زیرسلولی» مستقیماً اثبات شده و می‌توانی آن را در متن بنویسی.
- [ ] **اجرای B همسان با A:** بلوک `if (GEOM == "none") { mx = 20e-9; mzmin = -20e-9; mzmax = 20e-9; }` را کامنت کند و `dip_z` را برای none برابر `35e-9` بگذارد. اگر F_p,B همچنان نوسان داشت → `FDTD_SPAN = 1.2e-6` و `pml layers = 32`.
- [x] ~~برازش طلا~~ — در متن به‌عنوان «تنظیم پیش‌فرض، کمّی‌نشده، کران بالای ۱۴٪ از مقایسه با می» بسته شد.

## 🟠 اولویت ۲ — خودت می‌توانی انجام بدهی (بدون لومریکال)

- [x] مقایسه‌ی کمّی با Mohammadi, Sandoghdar & Agio (NJP 2008): آن‌ها برای نانومیله‌ی طلا با گاف چند نانومتر F_p ~ ۱۰³ و η در حد چند درصد گزارش کرده‌اند. یک جمله با عدد دقیق آن مقاله (شکل ۳ و ۴ آن مقاله) به بخش ۴٫۱ اضافه کن.
- [x] ~~برای حالت D، حل می را با گاف‌های ۳، ۱۰، ۲۰ nm هم اجرا کن~~ (انجام شد) (`rates_radial(lam, 15.874, 15.874+gap)`) و یک شکل «η بر حسب گاف (تحلیلی)» بساز — این تا وقتی حالت‌های ۹–۱۱ اجرا نشده، جایگزین موقت است.
- [ ] `analysis/mie_dipole_sphere.py` را با `nmax` = ۳۰ و ۶۰ اجرا کن و همگرایی سری را در یک خط در متن ذکر کن (الان ۶۰ است؛ در ۵۰۸ nm با ۳۰ هم همگراست).
- [x] چکیده را یک‌بار دیگر بخوان: معادل انگلیسی «قرارگیری زیرسلولی (sub-cell placement)»، «برون‌کوپل‌شدن (out-coupling)» را در اولین کاربرد بیاور.
- [ ] تاریخ روی جلد («مهر ۱۴۰۵») را با تاریخ ارسال واقعی هماهنگ کن.
- [ ] `first artcel text.txt` و `draft/*.md` دیگر منبع اعداد نیستند؛ یا حذفشان کن یا در README بنویس «منسوخ — فقط تاریخچه».

## 🟡 اولویت ۳ — پیش از ارسال به ژورنال

- [ ] (تنها کار فنی باقی‌مانده، اختیاری) کامپایل XeLaTeX واقعی برای PDF حروف‌چینی‌شده — در این محیط TeX نصب‌شدنی نبود؛ فعلاً PDF را از HTML نهایی با Print بگیر. کامپایل XeLaTeX (روی سیستمی با TeX Live یا با داکر `docker/`) و چک‌کردن: جدول ۴ (`p{}` ستون‌ها)، شکل‌ها، `\footnote` داخل متن فارسی، شماره‌ی مراجع با biber.
- [ ] Purcell 1946: بهتر است در bib بنویسی `pages = {681}, note = {Abstract B10}`.
- [x] کلیدواژه‌ها به ۶ مورد کاهش یافت؛ اگر ژورنال ۵ می‌خواهد، «ناهمسانگردی هندسی» و «خاموشی» را نگه دار.
- [x] یک‌بار کل متن را برای واژه‌ی «تست» جست‌وجو کن (نباید باقی مانده باشد).

---

## راهنمای بازسازی خروجی‌ها (هر بار که چیزی عوض شد)

```bash
python3 analysis/mie_dipole_sphere.py     # شکل می + جدول متنی analysis/D_sphere_mie_vs_fdtd.txt
python3 analysis/make_run2_figures.py     # چهار شکل داده‌محور
python3 tools/make_final_html.py          # مقاله_نسخه_نهایی.html (نسخه‌ی نهایی تک‌فایلی)
python3 tools/tex2html.py                 # manuscript/preview.html (نسخه‌ی بازبینی)
python3 tools/make_review_zip.py          # maghale-preview.zip
rm -rf maghale-preview && mkdir maghale-preview && cd maghale-preview && unzip -q ../maghale-preview.zip && mv maghale-preview/* . && rmdir maghale-preview
```
